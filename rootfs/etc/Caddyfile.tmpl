{{ $cfg := .Cfg }}

{{/* HealthCheck  */}}
{{ .HealthzHost }}:{{ .HealthzPort }} {
  status 200 {{ .HealthzPath }}
}

{{/* Forward Real IP */}}
realip {
  {{ if $cfg.UseProxyProtocol }}
    from {{ $cfg.ProxyRealIPCIDR }}
  {{ else }}
    from 0.0.0.0/0
  {{ end }}
}

{{ range $name, $upstream := .Backends }}

{{ end }}
